# 아키텍처 설계서 — 목차 확정본

작성 2026-08-08 · v1.0 (목차 단계)
근거 = `REDESIGN_요구사항.md` v0.4 (1절 D6~D8 / 3절 R-A1~A11), `_legacy\docs\*`, `_legacy\README.md`
성격 = **구조 설계서.** 전략 로직은 다루지 않는다(정독 미완으로 근거 없음).

---

## 0. 문서 개요

- **0.1 목적** — 과거 실패를 **구조로 차단하는** 아키텍처 정의. 부채 관리 계획서가 아니다.
- **0.2 범위**
  - 포함: **D6~D8**(구조적 부채·증상 처리·검증 우회) → **R-A1~A11** 구조적 대응
  - 제외: **D1~D5**(전략 층 — 책 정의 오해, 교재 간 동일화). 정독 미완으로 근거 없음
  - 제외: 모듈 내부 알고리즘·매매 로직. **인터페이스와 경계만** 정의
- **0.3 추적 원칙** — 모든 절은 **D#(진단) → R-A#(요구사항) → 구조적 대응**의 사슬을 갖는다.
  대응 R-A가 없는 절은 넣지 않는다.
- **0.4 참조 문서** — 위 근거 4종에 한정. 외부 방법론·도구 문서를 근거로 쓰지 않는다.

---

## 1. 실패 진단 (D6~D8)

> ※데이터 정합성(trade_log NULL 21건·역산 325건·time 유실 47건)은 ROADMAP **Phase 0에서 해결 완료**.
>   본 문서가 다루는 것은 Phase 2(구조)가 미완으로 남긴 부채다.

- **1.1 D6 — 구조적 부채**
  - `index.html` 16,130줄 / `legacy_compat_api.py` 8,896줄 (단일 파일 비대화)
  - 키움 COM 동시접근 방지 = **플래그 기반 임시 우회**
  - **에러 복구 부재** — 장중 크래시 시 미체결 주문 방치
- **1.2 D7 — 증상 처리 반복**
  - `292061a` COM 크래시의 근본(큐 구조) 대신 `await` 임시 우회
  - `515f40c` DB 형식 불통일 근본 미해결, 프론트 파싱만 수정
- **1.3 D8 — 검증 우회**
  - `1866b35` snapshot 불일치를 `--no-verify` 강행
- **1.4 종합 판정** — ROADMAP Phase 2 자체 진단 인용:
  *"지금 구조에서는 버그 수정이 새 버그를 만든다."* → 해체 판단의 직접 근거

---

## 2. 검증 강제 구조 (R-A1, R-A10)

- **2.1 조용한 통과의 차단** [R-A1]
  - 길이·스키마·필수 필드 불일치 → **예외로 중단.** 기본값·부분 처리 금지
  - 선례: QUART `zip()` 길이 불일치로 정성 점수가 **경고 없이 누락**
- **2.2 검증 우회 경로 차단** [R-A10]
  - `--no-verify` 류 **기본 금지.** 예외는 명시 승인 + 사유 기록
- **2.3 ★이관 자산 연결** — 신규 구현이 아니라 **연결 지점 설계** 문제다
  - `check_integrity.py`(epoch-aware, 전항목 PASS) — 어느 단계에 강제 삽입할지
  - `pre_commit_check.py` — 커밋 게이트
  - `snapshot_test.py` — 변경 전후 대조 게이트

---

## 3. 결과 표현 구조 (R-A3, R-A4)

- **3.1 산출값의 필수 동반 정보** [R-A3]
  - 값 + **표본 수 · 신뢰구간 · 검정력**을 함께 반환. 숫자 단독 노출 금지
  - 선례: QUART 대부분 구성이 **검정력 부족**으로 결론 무의미
- **3.2 3값 결과 체계** [R-A4]
  - `통과 / 실패 / **판정 불가**` — 판정 불가를 **1급 결과**로 취급
  - 데이터로 갈리지 않는 것을 억지로 이분하지 않는다
- **3.3 ★이관 자산 — 서술 선례**
  - `PHASE_1A_DATA_SOURCES.md`가 곳곳에 **한계·주의·과최적화·무효**를 스스로 기록
    (표본 부족 셀 N=3·N=15, 섹터 매핑 11%, MDD −37% 구간, 배점 무효 판정)
  - ★**문체까지 계승한다.** 이것이 R-A3·R-A4의 실제 선례다

---

## 4. 데이터 경계 구조 (R-A2, R-A5, R-A6)

- **4.1 시계열 분할 강제** [R-A2]
  - 분할 함수는 **시간 기준만** 제공. **종목 기준 분할 API를 아예 노출하지 않는다**
  - 선례: 종목 기준 분할 → 데이터 누수
  - ★이관 자산: `_v_46_v4_walk_forward_validation` — **이미 이 수준을 충족한 작업**
- **4.2 상태 변경 추적** [R-A5]
  - DB `created_at` 필수 / `ORDER BY created_at DESC` / `LIMIT`은 `ORDER BY` 동반
  - 로그만으로 원인 단정 금지 — 설정·DB **직접 확인 경로** 제공
- **4.3 외부 API 계층화** [R-A6]
  - **1차 / 2차 / 캐시** 폴백 계층을 구조로 보유
  - 실패 시 "폐지·차단" 단정 없이 대안 경로 시도
  - ★**`pykrx` 사용 금지.** KRX는 `krx_api.py`(data-dbg.krx.co.kr Open API) 우선
  - 선례: FMP 유료 만료 · Akamai CDN 차단 · KRX 엔드포인트 변경
- **4.4 단위 표기 강제** (요구사항 R-S5의 구조 측면)
  - `value_mkrw`(백만 원) / `value_krw`(원) — **무단위 변수명 금지**
  - 근거: D3 거래대금 단위 = 백만 원(200억 = 20000)

---

## 5. 구조적 부채 차단 (R-A8, R-A9, R-A11)

- **5.1 모듈 크기 상한** [R-A8 ← D6]
  - 단일 파일 줄 수 상한 설정, 초과 시 **분리 강제**(pre-commit / CI 검사)
  - 16,130줄·8,896줄 재발 방지
- **5.2 임시 우회 금지** [R-A9 ← D7]
  - 근본 원인 미해결 상태의 우회를 **기본 금지**
  - 불가피 시 **부채 목록 등록 + 해소 기한 명시.** 조용한 우회를 허용하지 않는다
- **5.3 크래시 복구** [R-A11 ← D6]
  - 장중 **상태 파일 저장**(실행 중 주문 목록)
  - 재시작 시 미체결 주문 확인, **손절 주문 복구**
  - 복구 시나리오 테스트

---

## 6. 자기진화 루프 — 모듈 경계 (R-A7)

> 원 철학(전략 수립 → 시뮬레이션 → 결과 수집 → ML 학습 → 전략 수정)은 유지.
> 단 **각 단계의 입출력을 독립 검증**할 수 있어야 한다. 결합되면 어느 단계가 성과를 망쳤는지 분리 불가.
> ★**각 단계는 입출력 타입과 호출 규약만 정의한다. 내부 로직은 비워 둔다**(향후 단권화 교재의 `[정량화] 가능` 항목이 들어올 자리).

- **6.1 Stage 1 — 전략 수립**: 입출력 타입 / 호출 규약 / 독립 검증 방법
- **6.2 Stage 2 — 시뮬레이션**: 〃 ★이관 자산 `_v_47_v4_realistic_slippage`(슬리피지 0.76%) 연동
- **6.3 Stage 3 — 결과 수집**: 〃 (R-A5 상태 추적과 접점)
- **6.4 Stage 4 — ML 학습**: 〃 (R-A2 시계열 분할 강제 지점)
- **6.5 Stage 5 — 전략 수정**: 〃
- **6.6 단계 간 계약(Contract)** — 각 경계에서 R-A1(조용한 통과 금지)이 발동하는 지점 명시

---

## 7. 거버넌스 (ADR)

- **7.1 ADR 도입** — 설계 변경·핵심 결정마다 **근거 / 대안 / 영향도** 기록
- **7.2 원칙 연결** — 프로젝트 기본 원칙 *"모든 값에는 출처가 있어야 한다"*(R-S1)의 구조 측면
- **7.3 부채 목록** — 5.2에서 등록된 우회 항목의 관리 대장

---

## 8. 부록

- **8.1 R-A ↔ 절 대응표** — 11개 항목이 빠짐없이 배치됐는지 검증하는 체크리스트
- **8.2 이관 자산 활용 현황** — `_legacy\` 81파일 중 본 설계에 연결된 항목
- **8.3 미결 사항** — 폐기·이관 경계(M3), `_v_01~_v_67` 개별 재사용 가치 미평가

---

## ✅ R-A 배치 검증

| 항목 | 배치 절 |
|---|---|
| R-A1 조용한 통과 금지 | 2.1, 6.6 |
| R-A2 시계열 분할 강제 | 4.1, 6.4 |
| R-A3 표본·검정력 동반 | 3.1 |
| R-A4 3값 결과 | 3.2 |
| R-A5 상태 추적 | 4.2, 6.3 |
| R-A6 API 계층화 | 4.3 |
| R-A7 루프 단계 독립 검증 | 6장 전체 |
| R-A8 모듈 크기 상한 | 5.1 |
| R-A9 임시 우회 금지 | 5.2, 7.3 |
| R-A10 검증 우회 차단 | 2.2 |
| R-A11 크래시 복구 | 5.3 |

**11/11 배치 완료 · 누락 0**
