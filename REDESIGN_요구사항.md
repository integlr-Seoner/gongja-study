# 단권화 및 시스템 재설계 — 요구사항 정의서

작성 2026-08-07 · **v0.4**
(v0.1 → v0.2 단권화 중심 재구성 → v0.3 기존 문서 실사 → **v0.4 이관 실행 완료 · 스크립트 실측 반영**)
배치 권장 위치: 정독 저장소 루트 또는 `docs/`
상태: **착수 전 문서.** 착수는 양 교재 정독 완료 이후. 단 3절(아키텍처)은 지금 착수 가능.

---

## 0. 목적과 최종 산출물

**최종 목표는 프로그램이 아니라 「나만의 책」(단권화 교재)이다.**
프로그램은 그 책의 파생물이며, 책의 `[정량화] 가능` 항목만 코드로 옮긴 것이다.

```
금융공자 가이드북 ─┐
미라클 전자책     ─┼→ 개념 카드 대조 → 실증 검증 → 용어 통일 → 【단권화 교재】 → 프로그램
(향후 타 교재)    ─┘
```

책을 먼저 쓰는 것에는 부수 효과가 있다 — **설명 가능한 형태로 쓰는 과정 자체가 최고의 검증**이다.
정독 노트가 "읽었다"의 기록이라면, 책은 "이해했다"의 증명이다.

---

## 1. 진단 — 무엇이 무너졌는가

### ★1-0. 층위 구분 (v0.3 정정)

**기존 시스템의 문제는 단일하지 않다. 층에 따라 성패가 갈린다.**
2026-04 작성 문서(`PHASE_1A_DATA_SOURCES.md` 98.5KB, `V4_PATCH_DESIGN` v1~v3) 실사 결과:

| 층 | 판정 | 처분 |
|---|---|---|
| **구조** (단일파일 16,130줄·8,896줄, COM 임시우회, 에러복구 부재) | **실패** | **해체** |
| **구 점수 체계** (100점 배점, C3 눌림목 15점 등) | **실패** | 폐기 (V4가 대체) |
| **검증 방법론** (V4 카운트 점수, 시기분할, 13년 전수, 한계 명기) | ★**성공** | **이관** (2절) |
| **데이터 정합성 도구** (`check_integrity.py`, 체결 대조 자동화) | ★**성공** | **이관** |

→ ★**"근거 없이 임의값으로 채웠다"는 진단은 V4 이전 시기에 한정된다.**
   2026-04 V4 작업은 오히려 본 문서가 요구하는 방식의 **모범 사례**다(2절 참조).
   개발이 2026-04-22에 멈춘 것은 방법이 틀려서가 아니라 **검증된 V4를 담을 구조가 없어서**로 보인다.
   패치 설계서가 *"기존 코드 무수정, 회귀 위험 0"* 을 강조한 것 자체가 구조를 신뢰하지 못한 흔적이다.

### 1-1. 입력이 틀렸다 (V4 이전 시기)

기존 StockAnalyst 초기 구축은 금융공자 가이드북 + 미라클 전자책을 토대로, 여기에 다른 지표를 추가해 이루어졌다.
**그러나 그 "토대"에 해당하는 책의 정의 자체를 부정확하게 이해한 상태였다.**
추정이 아니라 2026년 7~8월 정독 프로젝트에서 **실증된 사실**이다:

| # | 실증 내용 | 근거 |
|---|---|---|
| **D1** | **적정 거래대금**의 정의가 트랙마다 달랐다. "상승을 위한 최소 거래대금(관심도에 따라 증가)"으로 확정된 것은 스윙 트랙 정독 시점 | 92 C01 |
| **D2** | **낙폭과대**가 단타(분봉, 1~10분 −6~−16%)와 스윙(일봉, C < H×0.85)에서 **시간축이 완전히 다르다.** 충돌이 아니라 별개 적용 | 92 C03 / 단타 G1799·G1800 ↔ 스윙 G4895·G4900·G4907 |
| **D3** | **거래대금 단위 = 백만 원**(200억 = 20000)은 수식관리자 트랙에서야 확정. 모르고 코딩하면 10⁶배 오차 | 수식관리자 트랙 |
| **D4** | **기준봉**의 본진(스윙 「기준봉 캔들 패턴 추적」 Part 01~03, 565p)은 재설계 판단 시점까지 **미독** | 코퍼스 실측 2026-08-04 |
| **D5** | **교재 간 기법 동일화 오류.** 한 저자 안에서도 트랙별 정의가 갈렸으므로(D2), 저자가 다르면 갈릴 여지가 더 크다. 세 유형: ⓐ**동명이의**(같은 용어·다른 뜻) ⓑ**이명동의**(다른 용어·같은 뜻 → 점수 엔진에서 **이중 계상**) ⓒ**전제 조건 차이**(시간축·시장 국면·종목군·자금 규모·도구) | D2 유추 + 낙폭과대 실증 3건이 전부 2022-09 CPI 쇼크 급락장 표본(CASE-0011~0013) |

### 1-2. 두 종류의 임의값이 섞였다

(a) 책을 잘못 이해한 값 / (b) 애초에 책에 없는 값.
섞이면 **결과가 나빠도 원인 분리가 불가능**하다. QUART에서 데이터 누수와 `zip()` 절단이 겹쳤을 때와 동일 구조.

### 1-3. 정성 서술의 무단 정량화

책이 정성적으로만 서술한 것("세력의 의도가 보이는 봉")을 그럴듯한 지표로 근사하면
그 지점은 검증 불가능한 블랙박스가 된다. "빈 껍데기 분석 프로그램"의 직접 발생 경로.

### 1-4. 구조적 부채 (2026-02-23 자체 진단)

`ROADMAP_INTERNAL_QUALITY.md` 기록. Phase 0·1은 대부분 완료, **Phase 2(구조) 전 항목 미완**,
Phase 3(신뢰)은 Phase 2 전제라 착수 불가 상태.

| # | 내용 | 근거 |
|---|---|---|
| **D6** | **단일 파일 비대화** — `index.html` 16,130줄 / `legacy_compat_api.py` 8,896줄. 키움 COM 동시접근 방지가 **플래그 기반 임시 우회**. **에러 복구 없음**(장중 크래시 시 미체결 주문 방치) | ROADMAP §코드 구조 |
| **D7** | **증상 처리 반복** — 근본 원인 대신 우회. 커밋 `292061a`: COM 크래시의 근본(큐 구조) 대신 `await` 임시 우회 / `515f40c`: DB 형식 불통일 근본 미해결, 프론트 파싱만 수정 | ROADMAP §이번 세션 반성 |
| **D8** | **검증 우회** — 커밋 `1866b35`: snapshot 불일치를 `--no-verify` 강행 | 동일 |

★D7의 반복이 곧 ROADMAP Phase 2의 자체 진단으로 이어진다 —
  *"지금 구조에서는 버그 수정이 새 버그를 만든다."* 이것이 해체 판단의 직접 근거다.

---

## 2. ★이관 자산 (폐기하지 말 것)

> v0.3 신설. "코드 전량 폐기"는 **철회**한다. 아래는 다시 만들 수 없거나 재사용 가치가 높다.

### 2-1. V4 카운트 기반 점수 체계 — 검증 완료

**정의**: 정배열+장대양봉 / 60일 신고가 / 거래대금 3배↑ / 종가 95%↑ — **4조건 각 1점 → 0~4 정수**

| 측정 | 결과 |
|---|---|
| 단조 증가성 | **역전 0회** (구 체계 2회) |
| score==4 실현수익 | **+3.044%** (mean +3.504%, conservative +2.744%) |
| score==4 승률 | **63.1%** |
| CAGR / MDD / Sharpe | **+43.72% / −1.6% / 8.90** (Mode A) |
| 시기 분할 | **STABLE** (P1/P2/P3 cv=0.14) |
| 연도별 | **13/13 연도 양수** |
| score==3 / ≤2 | +0.370% / **음수** |

★시기 분할 안정성 + 13개 연도 전수 양수는 **부칙 P-2의 "기간 분할 부호 불변" 요건과 동일한 검증**이다.

### 2-2. ★V4 = 기준봉 (독립 경로 수렴)

`PHASE_1A_DATA_SOURCES.md` §: *"C1+C2+V 조합 = 사실상 기준봉 패턴, 갭업 5%+ 포착률 9.30%로 최고"*

→ **거래대금 + 장대양봉 조합이 기준봉이라는 결론에 2026-04에 데이터로 독립 도달**했다.
   현재 스윙 트랙 정독에서 확정된 기준봉 정의(*추세 자체를 바꾸는 캔들*, 거래대금+장대양봉)와 **동일 방향**이다.
   → R-S9 참조. **R-S6 등가성 검증의 최우선 대상.**

### 2-3. 구 점수 체계의 실패 원인 (교훈으로 보존)

- **C3(눌림목)은 약한 조건이 아니라 해로운 조건** — realized **−0.17%**
- **신고가 + 눌림목 동시 충족은 논리 모순** (합산 로직 미비 → 점수 인플레)
- 종가 95%↑ 가 실제 강한 신호인데 배점 10점으로 과소
- [70-80] 구간 realized **−0.16%** = 고득점이 저수익 → **점수-수익 단조성 붕괴**

★교훈: **가중 합산 배점(100점제)은 모순 조건을 걸러내지 못한다.** V4의 카운트 방식이 이를 해소했다.
  → R-S10.

### 2-4. 검증 스크립트군 · 정합성 도구

★**2026-08-07 이관 완료** → `book_extracts\_legacy\` (총 81파일 780KB, 매니페스트 = 동 폴더 `README.md`)
  ※`book_extracts` 하위이므로 정독 저장소 분리 시 **함께 이동**한다.

**`verify_scripts/` — `_v_01`~`_v_67` 전량 66개 (429KB)**
당초 v0.2에서 `_v_25`~`_v_34`만 파악했으나, 실사 결과 **작업이 훨씬 더 진행돼 있었다.**
V4 검증에서 멈춘 것이 아니라 **워크포워드·슬리피지·포트폴리오 배분까지 도달**했다.

| 스크립트 | 내용 |
|---|---|
| `_v_25_score_redesign` | 구 100점 체계 vs 신 체계 단조 증가성 비교 |
| `_v_26_count_based_score` | ★V4 카운트 방식 도입 (역전 0회) |
| `_v_27_v4_backtest_period` | ★시기 분할 STABLE, 13/13 연도 양수 |
| `_v_33` / `_v_34` | 섹터 · 가격×거래대금 교차 (표본 한계 명기) |
| `_v_35`, `_v_45` | 시장 국면별 / 국면 동적 파라미터 |
| **`_v_46_v4_walk_forward_validation`** | ★★**워크포워드 검증** |
| **`_v_47_v4_realistic_slippage`** | ★슬리피지 0.76% 반영 |
| **`_v_43` / `_v_44`** | ★V4 + H3S2 **결합 포트폴리오 · 최적 배분** |
| `_v_14`~`_v_18` | KRX Open API · 키움 NXT API 검증 |
| `_v_52`~`_v_64` | DB 프로브 · 매수 게이트 추적 · 진단 도구 |

→ ★이는 1-0절의 "검증 방법론 층 = 성공" 판정을 **당초 판단보다 더 강하게** 뒷받침한다.
   워크포워드·슬리피지·결합 포트폴리오는 본 문서 R-A2·R-A3가 요구하는 수준을 이미 충족한 작업이다.

**`tools/` — 4개 (136KB)**
`check_integrity.py`(epoch-aware, 전항목 PASS, daily_scheduler step 12 연동) ·
`snapshot_test.py` · `pre_commit_check.py` ·
`closing_bet_unified.py`(V4 로직 원본, 70,896 chars — `predict_v4()` 대상 파일)

**`docs/` — 11개 (215KB)**
`PHASE_1A_DATA_SOURCES.md`(98.5KB/2,486줄) · `V4_PATCH_DESIGN` v1~v3 ·
`V4_STAGE3_AUTOTRADER_DESIGN` · `V4_CHECKPOINT_20260422` · `ROADMAP_INTERNAL_QUALITY` ·
`SESSION_START` · `BEFORE_CODING` · `BACKTEST_DESIGN` · `BACKTEST_REPORT_20260418`

**★미이관 1건 — 체결 대조 자동화** `_run_trade_reconciliation`(키움 체결내역 vs trade_log)은
애플리케이션 코드 내부 함수로 폐기 대상 파일에 포함돼 있다. **로직만 재구현**할 것(R-A5 관련).

### 2-5. 데이터 (백업 1부, 활용 판단 보류)

`trade_log` 947건 / `strategy_performance` 20,003건 / `training_data_v2` 3,102건.
정합성 보정 완료분(NULL 21·역산 325·time 47건).
※ 폐기 예정 전략이 생성한 결과물이므로 새 전략 검증에는 직접 쓸 수 없다.
   활용 가치는 ⓐ파이프라인 테스트용 실데이터 ⓑ**실패 사례 표본**(사례대장 `실패` 라벨과 동성격) 두 가지.

### 2-6. 방법론 자체

`PHASE_1A_DATA_SOURCES.md`는 곳곳에 **한계·주의·과최적화·무효**를 스스로 기록한다
(표본 부족 셀 N=3·N=15, 섹터 매핑 11%뿐, MDD −37% 구간, 배점 무효 판정 등).
★이 서술 습관이 본 문서 R-A3·R-A4의 실제 선례다. **문체까지 계승할 것.**

---

## 3. 아키텍처 요구사항 (R-A)

> 착수 조건: **없음. 전략과 독립이므로 지금 설계 가능.**
> 전략을 모르는 상태에서 설계하는 것이 오히려 장점이다(특정 전략에 맞춘 구조 왜곡 방지).

**R-A1. 검증 실패는 조용히 통과하지 않는다**
QUART 교훈: `zip()` 길이 불일치로 정성 점수가 경고 없이 누락.
길이·스키마·필수 필드 불일치는 **예외로 중단**한다. 기본값·부분 처리로 넘기지 않는다.

**R-A2. 시계열 분할을 구조로 강제한다**
QUART 교훈: 종목 기준 train/test 분할 → 데이터 누수.
분할 함수는 **시간 기준만** 제공하고 종목 기준 분할 API를 노출하지 않는다.

**R-A3. 표본 수·검정력을 산출 단계에서 보고한다**
결과는 값과 함께 **표본 수·신뢰구간·검정력**을 동반 반환한다.
검정력 미달 시 **"판정 불가"** 플래그 부착(숫자 단독 노출 금지). ※선례 = 2-6.

**R-A4. 결과는 세 값으로 보고한다**
`통과 / 실패 / 판정 불가` — **"판정 불가"를 1급 결과로** 취급한다.

**R-A5. 상태 변경은 추적 가능해야 한다**
DB `created_at` 필수, `ORDER BY created_at DESC`, `LIMIT`은 `ORDER BY` 동반.
로그만으로 원인 단정 금지 — 설정·DB 직접 확인 경로 제공.

**R-A6. 외부 API 의존을 계층화한다**
1차/2차/캐시 폴백 계층을 구조로 갖고, 실패 시 "폐지/차단" 단정 없이 대안 경로를 시도한다.
※ pykrx 금지, KRX는 `krx_api.py`(data-dbg.krx.co.kr Open API) 우선.

**R-A7. 자기진화 루프의 각 단계를 독립 검증 가능하게 한다**
원 철학(전략 수립 → 시뮬레이션 → 결과 수집 → ML 학습 → 전략 수정) 유지.
단, **각 단계 입출력을 독립 검증**할 수 있어야 한다.

**R-A8. ★모듈 크기 상한을 정하고 강제한다** (D6)
단일 파일 줄 수 상한을 설정하고 초과 시 분리를 강제한다(CI/pre-commit 검사).
16,130줄·8,896줄 재발 방지.

**R-A9. ★임시 우회 금지 — 우회 시 부채로 등록한다** (D7)
근본 원인 미해결 상태의 우회(`await` 삽입, 프론트 파싱 보정 등)를 기본 금지한다.
불가피하면 **부채 목록에 등록**하고 해소 기한을 명시한다. 조용한 우회를 허용하지 않는다.

**R-A10. ★검증 우회 경로를 차단한다** (D8)
`--no-verify` 류 우회를 기본 금지. 예외는 **명시 승인 + 사유 기록**이 있을 때만.

**R-A11. ★크래시 복구를 갖춘다** (D6 / ROADMAP Phase 1-3 미완)
장중 상태 파일 저장(실행 중 주문 목록), 재시작 시 미체결 주문 확인, **손절 주문 복구**,
복구 시나리오 테스트.

---

## 4. 단권화·전략 요구사항 (R-S)

> 착수 조건: **양 교재 정독 완료** + 부칙 P-2 검증.

**R-S1. 모든 파라미터에 출처를 부여한다**
① **G번호/M번호**(책 근거) ② **검증 결과**(P-2 산출) ③ **사용자 명시 지시** — 셋 중 하나.
어디에도 없으면 **코드에 넣지 않는다.** 필요하면 질문한다.

**R-S2. 책 근거와 추가 지표를 구조적으로 분리한다**
별개 모듈·별개 설정. 성과를 계열별로 분리 측정할 수 있어야 한다.

**R-S3. 개념의 출처·트랙 귀속을 명시한다**
예: `낙폭과대(스윙/일봉, G4900)` — `낙폭과대(단타/분봉, G1800)`와 교차 사용 금지.

**R-S4. 정량화 불가 영역은 코드로 만들지 않는다**
91 `[정량화]`의 `불가(수동 판단 영역)` 항목은 **수동 판단 지점으로 명시 노출**한다.
자동화율을 낮추더라도 블랙박스를 만들지 않는다.

**R-S5. 단위를 이름·타입으로 강제한다**
`value_mkrw`(백만 원) / `value_krw`(원). 무단위 변수명 금지. (D3)

**R-S6. 교재별 기법은 통합 전 등가성 검증을 거친다**

| 단계 | 방법 |
|---|---|
| ① 정의 대조 | 8필드 카드를 양쪽 동일 형식으로 만들어 **필드별로 나란히** 비교 |
| ② 실증 대조 | 같은 KRX 데이터에 두 정의를 각각 적용 → **선별 종목·날짜 겹침률** 산출 |
| ③ 판정 | **동일 / 별개 / 판정 불가** 3값 + 근거 기록 |

★②가 핵심. 문헌 대조는 "같아 보인다"에서 멈추지만 데이터 적용은 정량적으로 갈린다.
★**판정 전 코드화 금지.** "둘 다 좋으니 합치면 낫겠지"는 검증되지 않은 가정이다.

**R-S7. 단권화는 통합이 아니라 배치다**
전제가 다른 정의를 **평균 내지 않는다**(양쪽 다 망가진다). 적용 조건을 명시해 병존시킨다.

```
낙폭과대
├ 분봉 적용 (1~10분 −6~−16%)  — 단타 국면   G1800
└ 일봉 적용 (C < H×0.85)       — 스윙 국면   G4900
```

**R-S8. 용어 채택 규칙**

**[보편성 정의]** 다수가 사용하거나 통설인 표기.

| 순위 | 확인 출처 | 비고 |
|---|---|---|
| ① | 제도·기관 용어 (KRX·금투협·금감원) | 다툼 여지 거의 없음 |
| ② | HTS 실제 표기 (키움 등) | **API 필드명과 직결** → 프로그램 단계에서 ①보다 실용적일 수 있음 |
| ③ | 다수 저자 사용 | ★**"N개 출처 중 M개"** 로 세어 기록. 인상이 아니라 계수 결과 |
| ④ | 검색 빈도 | 보조 지표. **단독 근거 불가** |

**[채택 순서]** 보편 용어 → 개념 정의에 가장 가까운 용어 → 신규 정의
**[저자 고유 용어 예외]** 저자 고유 표기(적정 거래대금·N자 파동·기준선 매매·짝짓기 매매)는
  **저자 용어를 표제어로 유지** + 설명적 보편 표현 병기. 예: `N자 파동 (= 이중 바닥 후 재상승 구조)`
**[불변 규칙]**
- 원 용어를 출처와 함께 병기하고 **삭제하지 않는다.**
  `통일 용어 ← [금융공자: ○○ (G번호) / 미라클: △△ (M번호)]`
  이유: ⓐ원서 대조 ⓑ제3 교재 유입 시 판정 재사용 ⓒ**용어 선택이 틀렸을 때 되돌릴 수 있어야 한다**
- **보편성 판정 근거를 함께 기록**한다("KRX 표기" / "6개 출처 중 5개").

**R-S9. ★V4와 금융공자 기준봉을 최우선 대조한다** (2-2)
2026-04 V4 검증과 스윙 트랙 기준봉 정의는 **독립 경로로 같은 방향에 도달**했다.
→ R-S6 3단계 등가성 검증의 **1번 대상**으로 삼는다.
  대조 항목: 거래대금 배수(V4 3배↑ vs 책의 적정 거래대금) · 장대양봉 판정식 · 신고가 조건 유무 · 종가 위치.
  ★일치하면 상호 보강(문헌 근거 + 13년 실증), 불일치하면 **어느 쪽이 데이터에 맞는지 재검증**한다.

**R-S10. ★가중 합산 배점을 기본 채택하지 않는다** (2-3)
구 100점 배점은 **모순 조건(신고가+눌림목)을 걸러내지 못하고** 점수-수익 단조성이 붕괴했다.
조건 카운트 방식(V4)을 기본으로 하고, 가중치를 도입하려면 **단조 증가성 검증을 통과**해야 한다.

---

## 5. 착수 순서

| 단계 | 내용 | 선행 |
|---|---|---|
| 1 | **금융공자 완독** (스윙 → 재료매매 → 조건검색기, 약 3,400p 잔여) | — |
| 2 | 91 개념인덱스 `[정량화]` 완성 = 명세 원재료 | 1 |
| 3 | **미라클 전자책 정독** (동일 방식) | 1 |
| 4 | **개념 카드 대조** (R-S6 ①) — ★V4↔기준봉 우선(R-S9) | 2, 3 |
| 5 | **KRX 실증 대조** (R-S6 ②③) | 4 |
| 6 | **용어 통일** (R-S8) | 5 |
| 7 | ★**단권화 교재 집필 — 최종 산출물** | 6 |
| 8 | 전략 명세 — 교재의 `[정량화] 가능` 항목만 | 7 |
| 9 | 프로그램 구현 | 8 + A |
| **A** | **아키텍처 설계 (R-A)** — 1~8과 **병행 가능** | 없음 |

★A는 지금 착수 가능. 분산 작업(C안) 시 **두 번째 노트북 담당 후보**(정독과 파일·G번호 무충돌).
★7 이후 강의 단계 가능. 교재가 곧 강의 자료이며, **강의 중 막히는 지점이 교재의 빈 곳**이다.

---

## 6. 미결 / 확인 필요

- **M1.** ~~기존 문서 진단 미확인~~ → **v0.3에서 해소.** `ROADMAP_INTERNAL_QUALITY`·`PHASE_1A_DATA_SOURCES`·`V4_PATCH_DESIGN` 실사 완료, D6~D8 및 2절로 반영.
- **M2.** 미라클 전자책 정독 미착수. 분량·구성 미조사.
- **M3.** 폐기/이관 경계 — 2절이 1차 목록. **이관 자체는 2026-08-07 완료**(`book_extracts\_legacy\`, 81파일).
  단 `_v_01`~`_v_67` **66개의 개별 재사용 가치는 미평가** — 재설계 착수 시 선별할 것.
- **M4.** ~~미커밋 23건 보존 필요~~ → **철회.** 코드는 폐기 대상이므로 커밋 불요.
  단 **2절 이관 자산에 해당하는 파일**(검증 스크립트·정합성 도구)은 정독 저장소 또는 신규 저장소로 **복사 보존**할 것.
- **M5.** 단권화 교재 배포 여부 미정. 배포 가능성이 있다면 **원문 전사가 아닌 재구성 원칙**을 집필 초기부터 적용(사후 수정은 전체를 손봐야 함).
- **M6.** ~~`PHASE_1A_DATA_SOURCES.md` 원문 보존 필요~~ → **v0.4에서 해소.** `_legacy\docs\`로 이관 완료.
  본 문서에는 요지만 반영했으므로, V4 상세 수치·교차 매트릭스는 **원문(2,486줄)을 직접 참조**할 것.

---

## 7. 이 문서의 사용법

- 정독 중 새 실증이 나오면 **1절 표에 D# 행을 추가**한다.
- 요구사항 추가 시 R-S# / R-A# 번호를 이어서 부여한다.
- ★**원칙: 여기 적힌 요구사항도 근거가 있어야 한다.** 근거 없는 요구사항은 추가하지 않는다.
