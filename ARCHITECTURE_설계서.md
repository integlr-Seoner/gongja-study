# 아키텍처 설계서

작성 2026-08-08 · 본문 v0.1 (0~2장)
근거 = `REDESIGN_요구사항.md` v0.4 · `_legacy\docs\ROADMAP_INTERNAL_QUALITY.md` · `_legacy\docs\PHASE_1A_DATA_SOURCES.md` · `_legacy\README.md`

---

# 0. 문서 개요

## 0.1 목적

본 문서는 **과거 실패를 구조로 차단하는 아키텍처**를 정의한다.

부채 관리 계획서가 아니다. "무엇을 고칠 것인가"의 목록이 아니라, **"같은 실패가 다시 일어날 수 없게 만드는 구조"** 를 기술한다. 두 가지는 다르다. 전자는 개발자의 주의에 의존하고, 후자는 주의하지 않아도 막힌다.

이 구분이 중요한 이유는 진단 자체에 있다. `ROADMAP_INTERNAL_QUALITY.md`(2026-02-23)의 「이번 세션 반성」 표는 커밋 5건의 실패를 기록하는데, 그중 3건이 **알면서 우회한 사례**다. 즉 무엇이 옳은지 몰라서 실패한 것이 아니라, 옳은 길이 번거로워서 우회한 것이다. 그러므로 지식이나 규칙을 추가하는 대응은 효과가 없다. **우회 경로 자체를 없애야 한다.**

## 0.2 범위

**포함**

| 대상 | 내용 |
|---|---|
| 진단 | **D6~D8** — 구조적 부채 · 증상 처리 반복 · 검증 우회 |
| 요구사항 | **R-A1~A11** — 위 진단에 대한 구조적 대응 |
| 산출 | 강제 지점, 인터페이스, 모듈 경계, 거버넌스 |

**제외**

| 대상 | 사유 |
|---|---|
| **D1~D5** (전략 층) | 책 정의 오해·교재 간 동일화 문제. **정독 미완으로 근거 없음** |
| 모듈 내부 알고리즘 | 매매 로직은 단권화 교재의 `[정량화] 가능` 항목이 확정된 뒤에 들어온다 |
| 기술 스택 선정 | 구조가 확정된 뒤의 문제. 지금 정하면 구조가 스택에 끌려간다 |

★**전략을 모르는 상태에서 설계하는 것이 오히려 장점이다.** 특정 전략에 맞춘 구조 왜곡을 원천 차단한다. 기존 시스템의 실패 중 상당 부분이 "지금 쓰는 전략에 맞춰 구조를 구부린" 결과였다.

## 0.3 추적 원칙

모든 절은 다음 사슬을 갖는다.

```
D#(실패 진단) → R-A#(요구사항) → 구조적 대응
```

**대응하는 R-A가 없는 절은 넣지 않는다.** 일반적인 설계서 템플릿에 있다는 이유로 절을 추가하지 않는다. 근거 없는 절은 근거 없는 결정을 부르고, 그것이 이 프로젝트가 피하려는 실패다.

문서 말미(8.1)에 **R-A ↔ 절 대응표**를 두어 11개 항목의 배치 누락을 검증한다.

## 0.4 참조 범위

설계 근거는 아래 4종에 **한정**한다.

- `REDESIGN_요구사항.md` v0.4 (1절 D6~D8, 3절 R-A1~A11)
- `_legacy\docs\ROADMAP_INTERNAL_QUALITY.md` (2026-02-23 자체 진단)
- `_legacy\docs\PHASE_1A_DATA_SOURCES.md` (2,486줄 V4 검증 트랙)
- `_legacy\README.md` (이관 자산 경위)

외부 방법론·프레임워크 문서·일반 아키텍처 이론을 근거로 쓰지 않는다. 근거로 쓸 경우 반드시 **왜 이 프로젝트에 해당하는지**를 실패 이력과 연결해 명시한다.

---

# 1. 실패 진단 (D6~D8)

> ※**데이터 정합성은 본 문서의 대상이 아니다.** trade_log NULL 21건·profit 역산 325건(62.6%)·time 유실 47건(21%)은 ROADMAP **Phase 0에서 해결 완료**(전항목 PASS)됐다.
> 본 문서가 다루는 것은 **Phase 2(구조)가 미완으로 남긴 부채**다. Phase 3(신뢰)은 Phase 2를 전제로 하므로 착수조차 되지 못했다.

## 1.1 D6 — 구조적 부채

`ROADMAP_INTERNAL_QUALITY.md` §코드 구조 기록:

| 항목 | 수치 | 판정 |
|---|---|---|
| `index.html` | **16,130줄** (단일 파일) | 🔴 |
| `legacy_compat_api.py` | **8,896줄** (단일 파일) | 🔴 |
| 키움 COM 동시접근 방지 | 플래그 기반 **임시 우회** | 🔴 |
| 에러 복구 | **없음** — 장중 크래시 시 미체결 주문 방치 | 🔴 |

세 번째와 네 번째가 특히 무겁다. COM 보호가 임시 우회라는 것은 **동시 호출 시 크래시 가능성이 남아 있다**는 뜻이고, 에러 복구가 없다는 것은 **그 크래시가 곧 미체결 주문 방치로 이어진다**는 뜻이다. 두 결함이 직렬로 연결되어 실거래 리스크를 만든다.

파일 비대화는 그 자체보다 **파급 경로**가 문제다. 16,130줄 단일 파일에서는 어떤 수정도 영향 범위를 특정할 수 없고, 그 결과가 1.4의 종합 판정이다.

## 1.2 D7 — 증상 처리 반복

「이번 세션 반성」 표에서 근본 원인 대신 우회를 택한 사례:

| 커밋 | 내용 | 우회 방식 |
|---|---|---|
| `292061a` | COM 크래시 | 근본 원인(**큐 구조**) 대신 `await` 임시 삽입 |
| `515f40c` | 시간 표시 오류 | **DB 형식 불통일** 근본 미해결, 프론트 파싱만 수정 |

두 사례의 공통 구조는 **증상이 발생한 레이어에서 처리했다**는 것이다. COM 크래시는 호출 직렬화의 문제인데 호출부에서 `await`으로 눌렀고, 시간 표시는 DB 스키마의 문제인데 표시부에서 파싱으로 덮었다.

이 방식은 매번 성공한다. 증상은 사라지고 커밋은 통과한다. 그러나 근본 원인이 남아 있으므로 **다음 증상이 다른 곳에서 나타난다.** 이것이 반복되면 1.4가 된다.

## 1.3 D8 — 검증 우회

| 커밋 | 내용 |
|---|---|
| `1866b35` | snapshot 불일치 → **`--no-verify` 강행** |

검증 도구는 이미 존재했다(`snapshot_test.py`, `pre_commit_check.py`). 도구가 없어서가 아니라 **우회 경로가 열려 있어서** 발생한 실패다.

같은 표에 기록된 나머지 2건(`9709a33` SESSION_START 미독 / `38d7cfb` BEFORE_CODING 미독 + 기존 패턴 미확인)도 성격이 같다. 절차 문서는 있었으나 **읽지 않아도 진행이 가능**했다.

★**이것이 본 설계서의 핵심 명제로 이어진다.** 규칙·문서·도구가 있어도, 우회가 가능하면 우회된다. 그러므로 대응은 규칙 추가가 아니라 **경로 차단**이어야 한다.

## 1.4 종합 판정

`ROADMAP_INTERNAL_QUALITY.md` Phase 2 자체 진단:

> *"지금 구조에서는 버그 수정이 새 버그를 만든다."*

이 한 줄이 해체 판단의 직접 근거다. D6(영향 범위 특정 불가) + D7(증상 처리로 근본 원인 잔존) + D8(검증 우회 가능)이 결합하면, **수정이 개선을 보장하지 못하는 상태**가 된다. 이 상태에서는 노력의 총량과 무관하게 품질이 개선되지 않는다.

Phase 0·1이 대부분 완료됐음에도 Phase 2 전 항목이 미완으로 남고 개발이 2026-04-22에 멈춘 것은, 이 지점에서 더 나아갈 수 없었기 때문으로 판단된다.

★다만 **동시기 V4 작업은 성공적이었다**(`REDESIGN_요구사항.md` 1-0절, 2절). 검증 방법론 층은 이관 대상이며, 본 설계서는 그 자산을 **담을 구조가 없었던 문제**를 해결하는 것이 목적이다.

---

# 2. 검증 강제 구조 (R-A1, R-A10)

> 대응 진단: **D7**(증상 처리 — 실패가 조용히 통과) · **D8**(검증 우회)
> 설계 명제: **검증은 통과 여부를 보고하는 것이 아니라, 통과하지 못하면 진행을 막는 것이다.**

## 2.1 조용한 통과의 차단 [R-A1]

### 2.1.1 요구

길이·스키마·필수 필드 불일치는 **예외로 중단**한다. 기본값 대입·부분 처리·건너뛰기로 넘기지 않는다.

### 2.1.2 근거

QUART ML 파이프라인 감사에서 `zip()` 길이 불일치로 **정성 점수가 경고 없이 누락**된 사례가 확인됐다. `zip()`은 짧은 쪽에 맞춰 조용히 잘라내므로, 데이터가 빠져도 오류가 발생하지 않는다. 결과적으로 ML Veto 로직 전체가 무효화됐으나 **실행은 정상적으로 완료**됐다.

이 실패의 성격은 D7과 같다 — **문제가 표면에 드러나지 않아 처리되지 않았다.**

### 2.1.3 구조적 대응

| 원칙 | 내용 |
|---|---|
| **묵시적 절단 금지** | 길이가 다른 시퀀스의 결합은 `strict` 모드 또는 사전 길이 검증을 거친다 |
| **묵시적 기본값 금지** | 필수 필드 부재 시 기본값을 넣지 않고 중단한다. 기본값은 **명시적으로 선언된 선택 필드에만** 허용 |
| **부분 성공 금지** | 배치 처리 중 일부 실패 시, 성공분만 반영하지 않는다. 전체 롤백 또는 명시적 실패 보고 |
| **경고 승격** | 경고 수준 로그로 남기고 진행하는 처리를 금지한다. 경고가 필요하면 그것은 예외이거나, 예외가 아니면 로그도 불필요하다 |

★**적용 지점은 6.6(단계 간 계약)에서 구체화한다.** 각 모듈 경계가 이 원칙이 발동하는 자리다.

## 2.2 검증 우회 경로 차단 [R-A10]

### 2.2.1 요구

`--no-verify` 류의 우회를 **기본 금지**한다. 예외는 **명시 승인 + 사유 기록**이 있을 때만.

### 2.2.2 근거

`1866b35`에서 snapshot 불일치를 `--no-verify`로 강행했다. 검증 도구는 존재했으나 **한 개의 플래그로 무력화 가능**했다.

### 2.2.3 구조적 대응

| 원칙 | 내용 |
|---|---|
| **우회 플래그 차단** | 커밋 훅을 서버 측(또는 CI)에도 두어, 로컬 우회가 통과해도 이후 단계에서 걸린다 |
| **우회 시 기록 강제** | 불가피한 우회는 사유·승인자·해소 기한을 남긴다 → **5.2 부채 목록과 동일 경로** |
| **검증 결과의 가시화** | 검증 생략 상태가 산출물에 **표기**되어야 한다 |

### 2.2.4 ★선례 — 검증 상태 표기

정독 프로젝트의 `_check_gspine.py`가 이미 이 패턴을 구현하고 있다. `--full` 미지정 시 결과 줄에 다음이 붙는다:

```
RESULT: OK - spine intact & coverage complete · [3] SKIP(--full 시 검사)
```

즉 **"통과"와 "일부 검사를 건너뛴 통과"가 결과 문자열에서 구별된다.** 이 표기가 없으면 부분 검증을 전량 통과로 오인하게 된다.

★본 시스템의 모든 검증 산출물은 이 형식을 따른다 — **무엇을 검사했고 무엇을 건너뛰었는지가 결과에 드러나야 한다.**

## 2.3 이관 자산 연결

★**이 절의 요구사항은 신규 구현 대상이 아니다.** 강제 지점이 이미 존재하며, 문제는 **어디에 연결할 것인가**다.

| 자산 | 현재 상태 | 연결 설계 |
|---|---|---|
| `check_integrity.py` | epoch-aware, 전항목 PASS. `daily_scheduler` **step 12** 연동 완료 | 스케줄러 외에 **파이프라인 각 단계 종료 시점**에도 삽입할지 결정 필요 |
| `pre_commit_check.py` | 존재. ROADMAP §0-5에 *"연동 별도 확인 필요"* 로 **미완 표기** | 커밋 게이트로 확정. 2.2.3의 서버 측 이중화 대상 |
| `snapshot_test.py` | `compare` 기능 존재 | 변경 전후 대조 게이트. `1866b35`에서 우회된 바로 그 검증 |

**미결 사항**
- `check_integrity.py`의 검사 범위가 신규 구조의 데이터 모델을 포괄하는지 미확인 → 4장 확정 후 재검토
- `pre_commit_check.py` 연동 미완 항목(ROADMAP §0-5)의 현재 상태 미확인

---

*(3장 이후 계속)*

# 3. 결과 표현 구조 (R-A3, R-A4)

> 대응 진단: **D7**(증상 처리 — 결과의 불확실성이 드러나지 않아 잘못된 결정을 부름)
> 설계 명제: **숫자 하나만 반환하는 함수는 그 숫자를 믿어도 되는지를 호출자에게 알려주지 않는다.**

## 3.1 산출값의 필수 동반 정보 [R-A3]

### 3.1.1 요구

측정·검증·백테스트의 결과는 값과 함께 **표본 수 · 신뢰구간 · 검정력**을 반환한다. 숫자 단독 노출을 금지한다.

### 3.1.2 근거

QUART 통계 검정력 분석에서 **대부분의 구성이 현실적 팩터 알파를 탐지하기에 검정력이 부족**한 것으로 확인됐다. 즉 산출된 수치들이 "효과 없음"을 뜻하는지 "탐지 실패"를 뜻하는지 구별할 수 없는 상태였다.

문제는 산출 자체가 아니라 **표현**이었다. 값만 반환되면 호출자는 그것을 결론으로 다룬다. 표본 수가 3건이든 3,000건이든 반환 타입이 같기 때문이다.

### 3.1.3 구조적 대응

측정 결과의 반환 타입은 **값 단독을 허용하지 않는다.**

| 필드 | 내용 | 필수 |
|---|---|---|
| `value` | 측정값 | ✅ |
| `n` | 표본 수 | ✅ |
| `ci` | 신뢰구간 | ✅ |
| `power` | 검정력 | ✅ |
| `status` | 3값 판정 (3.2 참조) | ✅ |
| `note` | 한계·주의 서술 | 선택 |

★`n`·`ci`·`power`는 **선택 필드가 아니다.** 산출 불가한 경우 `None`을 넣는 것이 아니라, **산출 불가 자체가 `status = 판정 불가`의 사유**가 된다.

### 3.1.4 표본 부족의 임계

`PHASE_1A_DATA_SOURCES.md`는 표본이 얇은 셀을 스스로 표기하고 있다:

- 가격×거래대금 교차 매트릭스: score==4 전체 616건, **각 셀 평균 31건** → 개별 셀 통계 유의도 제한
- *"저가+초대형" **N=3**(너무 적음)*, *"저-중+초대형" **N=15**(경계 수준)*
- 섹터 분석: 실섹터 매핑 종목 157개(전체 1,472건의 **약 11%**), score==4 중 매핑 **26건(4.2%)**

★**임계값 자체는 본 설계서에서 정하지 않는다.** 근거 없는 수치 설정을 금지하는 원칙(R-S1)에 따라, 임계는 검정력 계산으로 도출하거나 사용자 지시로 확정한다. 구조가 요구하는 것은 **임계가 존재하고, 미달 시 `status`가 바뀐다**는 것뿐이다.

## 3.2 3값 결과 체계 [R-A4]

### 3.2.1 요구

결과는 `통과 / 실패 / **판정 불가**` 세 값으로 보고한다. **판정 불가를 1급 결과로 취급한다.**

### 3.2.2 근거

2값 체계(통과/실패)는 **데이터로 갈리지 않는 경우를 표현할 수 없다.** 표현할 수 없으면 둘 중 하나로 밀어 넣게 되고, 그 순간 "모른다"가 "안다"로 바뀐다.

이것은 통계적 문제일 뿐 아니라 **의사결정 문제**다. "실패"는 개선 대상이지만 "판정 불가"는 **표본을 더 모아야 하는 대상**이다. 둘을 구별하지 않으면 표본 부족을 로직 결함으로 오인해 엉뚱한 수정을 하게 된다.

### 3.2.3 구조적 대응

| 상태 | 의미 | 후속 행동 |
|---|---|---|
| `PASS` | 기준 충족이 데이터로 확인됨 | 진행 |
| `FAIL` | 기준 미달이 데이터로 확인됨 | 원인 규명·수정 |
| `INDETERMINATE` | **데이터로 갈리지 않음** (표본 부족, 검정력 미달, 산출 불가) | **표본 축적 또는 설계 재검토.** 수정 대상 아님 |

★`INDETERMINATE`는 오류가 아니다. **정직한 결과**이며, 이를 오류로 처리해 재시도하거나 기본값으로 대체하는 구조를 금지한다(R-A1 연계).

### 3.2.4 상위 집계 규칙

여러 하위 결과를 집계할 때 `INDETERMINATE`를 무시하지 않는다.

- 하위에 `FAIL`이 하나라도 있으면 → `FAIL`
- `FAIL`이 없고 `INDETERMINATE`가 있으면 → **`INDETERMINATE`** (`PASS`로 승격 금지)
- 전부 `PASS`일 때만 → `PASS`

★판정 불가를 통과로 흡수하는 집계는 D8(검증 우회)과 같은 성격이다.

## 3.3 이관 자산 — 서술 선례 계승

`PHASE_1A_DATA_SOURCES.md`는 결과를 제시할 때마다 **한계·주의·과최적화·무효**를 스스로 병기한다. 확인된 사례:

| 위치 | 자체 표기 |
|---|---|
| §15-D | *"C3(눌림목)은 '약한 조건'이 아닌 **'해로운 조건'**"* (realized −0.17%) |
| §15-D | *"신고가 + 눌림목 동시 충족은 **논리 모순**"* |
| §16-F | *"권고 1~3은 OVERHEATED 문제 해소에 효과적이나 **완전한 해결책은 아님**"* → 더 근본적 재설계 필요 |
| §백테스트 | *"MDD −37% 수준 — **큰 하락 회피 못 함**"*, *"실전 적용 시 전체 자본의 20~30%로 실험 권장"* |
| §섹터 | 매핑률 11%, score==4 중 4.2% — **한계 명시** |

★**문체까지 계승한다.** 결과 보고에 한계를 병기하는 것은 문서 작성 습관이 아니라 **R-A3·R-A4의 실제 구현**이다. 이 서술이 없었다면 위 수치들은 결론으로 소비됐을 것이다.

★본 시스템의 모든 산출물은 동일 원칙을 따른다 — **결과와 그 결과를 믿을 수 있는 범위를 함께 제시한다.**

---

# 4. 데이터 경계 구조 (R-A2, R-A5, R-A6)

> 대응 진단: **D7**(레이어를 넘나드는 증상 처리) · **D6**(경계 불명확에 따른 영향 범위 특정 불가)
> 설계 명제: **잘못된 사용을 금지하는 것보다, 잘못 사용할 수 없게 만드는 것이 강하다.**

## 4.1 시계열 분할 강제 [R-A2]

### 4.1.1 요구

분할 함수는 **시간 기준만** 제공한다. **종목 기준 분할 API를 아예 노출하지 않는다.**

### 4.1.2 근거

QUART ML 파이프라인 감사에서 **종목 기준 train/test 분할로 인한 데이터 누수**가 확인됐다. 같은 시점의 정보가 학습·검증 양쪽에 들어가 성능이 과대평가됐다.

★주목할 점은 이것이 **지식의 부재로 인한 실수가 아니라는 것**이다. 시계열 분할의 필요성은 널리 알려져 있다. 그럼에도 발생한 이유는 **종목 기준 분할이 API로 제공되어 호출 가능했기 때문**이다.

### 4.1.3 구조적 대응

| 원칙 | 내용 |
|---|---|
| **API 미노출** | 종목 기준 분할 함수를 제공하지 않는다. 경고·문서화로 대체하지 않는다 |
| **시간 파라미터 필수** | 분할 함수는 기준 시점(cutoff)을 **필수 인자**로 받는다. 기본값 없음 |
| **미래 참조 차단** | 학습 구간에서 cutoff 이후 데이터에 접근하면 **예외**(R-A1 연계) |

★"쓰지 말라"는 규칙이 아니라 **호출할 수 없는 구조**여야 한다. D8의 교훈 — 우회가 가능하면 우회된다.

### 4.1.4 이관 자산

`_v_46_v4_walk_forward_validation` — **워크포워드 검증이 이미 구현된 작업**이다. 본 절의 요구를 이미 충족했으므로, 신규 구현이 아니라 **표준 분할 인터페이스로 승격**할 대상이다.

## 4.2 상태 변경 추적 [R-A5]

### 4.2.1 요구

| 항목 | 내용 |
|---|---|
| 시각 기록 | DB `created_at` **필수** |
| 정렬 | `ORDER BY created_at DESC` |
| 제한 | `LIMIT`은 반드시 `ORDER BY` 동반 |
| 진단 | 로그만으로 원인 단정 금지 — **설정·DB 직접 확인 경로** 제공 |

### 4.2.2 근거

Phase 0에서 보정된 결함들이 전부 **추적 정보의 부재**였다:

- `trade_log.action` NULL 21건
- `date` 빈값 4건
- `SELL` profit=0 but pct≠0 **325건(62.6%)** — 역산 필요
- `trade_history` time=00:00 **47/224(21%)** — 시간 유실

특히 `515f40c`(D7)는 DB 형식 불통일을 프론트 파싱으로 덮은 사례로, **추적 정보가 부실하면 상위 레이어에서 보정하려는 압력이 생긴다**는 것을 보여준다.

### 4.2.3 구조적 대응

| 원칙 | 내용 |
|---|---|
| **NOT NULL 제약** | `created_at`·`action` 등 필수 컬럼에 DB 레벨 제약. 애플리케이션 검증에만 의존하지 않는다 |
| **형식 강제** | 시간 형식을 CHECK 제약 또는 트리거로 강제 (ROADMAP §2-3 미완 항목) |
| **정렬 없는 LIMIT 금지** | 쿼리 검사로 강제. `LIMIT` 단독 사용은 결과가 비결정적이다 |
| **원본 우선 진단** | 로그는 단서이지 근거가 아니다. `.env`·설정·DB를 직접 확인하는 경로를 항상 제공한다 |

★**보정은 발생 지점에서 한다.** 상위 레이어 보정은 D7의 재현이다.

## 4.3 외부 API 계층화 [R-A6]

### 4.3.1 요구

**1차 / 2차 / 캐시** 폴백 계층을 구조로 보유한다. 실패 시 "폐지·차단" 단정 없이 대안 경로를 시도한다.

★**`pykrx` 사용 금지.** KRX 데이터는 `krx_api.py`(**data-dbg.krx.co.kr Open API**) 우선.

### 4.3.2 근거

외부 의존이 실제로 끊긴 이력:

| 사건 | 성격 |
|---|---|
| FMP 유료 플랜 만료 | **권한 소실** → Finnhub 하이브리드 전환 |
| Akamai CDN 차단 | **네트워크 차단** → FRED → Yahoo Finance → 캐시 3단 폴백 구현 |
| KRX 엔드포인트 변경 | **경로 변경** |

세 사건의 공통점은 **호출 실패가 곧 서비스 중단은 아니었다**는 것이다. 대안 경로가 있었고, 실제로 폴백을 구현해 해결했다. 문제는 그 폴백이 **사건 발생 후 사후 대응으로 만들어졌다**는 점이다.

### 4.3.3 구조적 대응

| 원칙 | 내용 |
|---|---|
| **계층 선언 필수** | 모든 외부 데이터 소스는 1차·2차·캐시를 **선언**한다. 단일 소스 의존을 허용하지 않는다 |
| **실패 원인 단정 금지** | 호출 실패 시 "폐지/차단"으로 결론짓지 않는다. **대안 경로 → Open API → 승인 현황** 순으로 확인 |
| **키 확인 우선** | 외부 API 작업 전 `.env`에서 **실제 키 이름·값을 직접 확인**한다. 기존 코드의 URL·엔드포인트 사용 패턴도 확인 |
| **캐시의 지위** | 캐시는 폴백의 마지막 단계이며, **캐시 응답임이 결과에 표기**되어야 한다(2.2.4 표기 원칙) |
| **루프 호출 시 sleep** | 반복 API 호출에는 대기를 필수로 넣는다 |

### 4.3.4 금지 목록

| 금지 | 대체 |
|---|---|
| `pykrx` | `krx_api.py` (data-dbg.krx.co.kr Open API) 또는 직접 웹 파싱 |

★이 금지는 사용자 지시에 근거하며, 신규 개발에서도 유지한다.

## 4.4 단위 표기 강제

### 4.4.1 요구

단위 혼동 이력이 있는 값은 **단위를 변수명·타입에 포함**한다. 무단위 변수명을 금지한다.

```
value_mkrw   (백만 원)
value_krw    (원)
```

### 4.4.2 근거

**D3 — 거래대금 단위 = 백만 원**(200억 = 20000). 이 사실은 수식관리자 트랙 정독에 이르러서야 확정됐다. 모르고 코딩하면 **10⁶배 오차**가 발생하며, 그 오차는 값이 그럴듯한 범위에 있는 한 검출되지 않는다.

★이는 R-S5(전략 요구사항)이나 **구조로 강제해야 실효가 있으므로** 본 장에 배치한다.

### 4.4.3 구조적 대응

- 금액·비율·기간 등 단위가 있는 값은 **변수명에 단위 접미사**를 붙인다
- 외부 데이터 수집 지점에서 **단위를 명시적으로 변환·기록**한다
- 단위가 다른 값의 연산은 타입 수준에서 차단하거나, 불가할 경우 변환 함수를 경유하게 한다

---

*(5장 이후 계속)*

# 5. 구조적 부채 차단 (R-A8, R-A9, R-A11)

> 대응 진단: **D6**(단일 파일 비대화·에러 복구 부재) · **D7**(임시 우회 축적)
> 설계 명제: **부채는 생기지 않게 하는 것이 아니라, 생기면 즉시 보이게 하는 것이다.** 보이지 않는 부채가 D6·D7을 만들었다.

## 5.1 모듈 크기 상한 [R-A8]

### 5.1.1 요구

단일 파일 줄 수 상한을 설정하고, 초과 시 **분리를 강제**한다(pre-commit / CI 검사). 16,130줄·8,896줄의 재발을 막는다.

### 5.1.2 근거

`index.html` 16,130줄, `legacy_compat_api.py` 8,896줄은 하루아침에 그 크기가 되지 않았다. **매 커밋은 수십 줄씩만 늘렸고, 각각은 합리적이었다.** 상한이 없으면 개별적으로 타당한 증분이 누적되어 한계를 넘는다. 그 시점에는 이미 분리 비용이 감당 불가능하다.

즉 이것은 **판단의 문제가 아니라 임계의 문제**다. "너무 커지기 전에 나누자"는 판단은 매번 "아직은 괜찮다"에 진다. 임계가 강제되어야 한다.

### 5.1.3 구조적 대응

| 원칙 | 내용 |
|---|---|
| **줄 수 상한** | 파일별 상한을 설정한다. ★**구체 수치는 본 문서에서 정하지 않는다**(R-S1) — 기존 정상 모듈의 분포에서 도출하거나 사용자 지시로 확정 |
| **초과 시 차단** | 상한 초과 커밋은 pre-commit에서 거부. 통과하려면 분리해야 한다 |
| **서버 측 이중화** | 로컬 우회(2.2) 대비 CI에도 동일 검사 |
| **복잡도 병행** | 줄 수뿐 아니라 함수 길이·중첩 깊이 등 복잡도 지표 병행 검토(선택) |

★상한 강제는 "큰 파일 금지"가 아니라 **"큰 파일을 만들려면 명시적으로 우회를 기록해야 함"**을 의미한다(5.2 연계). 정당한 예외는 있을 수 있으나, 조용히 넘어갈 수는 없다.

## 5.2 임시 우회 금지·부채 등록 [R-A9]

### 5.2.1 요구

근본 원인 미해결 상태의 우회(`await` 삽입, 프론트 파싱 보정 등)를 **기본 금지**한다. 불가피하면 **부채 목록에 등록**하고 해소 기한을 명시한다. 조용한 우회를 허용하지 않는다.

### 5.2.2 근거

D7의 두 사례(`292061a`, `515f40c`)는 **부채로 기록되지 않았다.** 커밋 메시지에 "임시"라는 표시도, 해소 계획도 없었다. 그 결과 우회가 영구화됐고, 다음 개발자(혹은 미래의 본인)는 그것이 임시 조치였다는 사실조차 알 수 없었다.

★즉 문제는 우회 자체가 아니라 **우회가 보이지 않았다**는 것이다. 긴급 상황에서 임시 조치가 필요할 수 있다. 금지해야 하는 것은 **기록 없는 우회**다.

### 5.2.3 구조적 대응

| 원칙 | 내용 |
|---|---|
| **부채 목록 상주** | 프로젝트에 부채 대장을 두고 버전 관리한다(7.3) |
| **등록 필수 항목** | 우회 위치 · 근본 원인 · 사유 · 등록일 · **해소 기한** · 담당 |
| **기한 초과 가시화** | 해소 기한이 지난 부채는 검증 산출물에 **경고로 표기**(2.2.4 형식) |
| **커밋 연동** | 우회를 포함한 커밋은 부채 대장 갱신을 동반하도록 검사 |

★이 구조는 R-A10(검증 우회 기록)과 **동일 경로를 공유**한다. `--no-verify` 우회든 로직 우회든, 모든 우회는 같은 대장에 남는다.

## 5.3 크래시 복구 [R-A11]

### 5.3.1 요구

장중 상태 파일 저장(실행 중 주문 목록), 재시작 시 미체결 주문 확인, **손절 주문 복구**, 복구 시나리오 테스트.

### 5.3.2 근거

D6의 네 번째 항목 — *"에러 복구 없음: 장중 크래시 시 미체결 주문 방치"*. 이것은 ROADMAP Phase 1-3에 항목으로 존재했으나 **미완**으로 남았다:

```
### 1-3. 장중 크래시 복구
- [ ] auto_trader 상태 파일 저장 (실행 중이었던 주문 목록)
- [ ] 프로그램 재시작 시 미체결 주문 확인 로직
- [ ] 손절 주문 복구 (크래시 전 걸어놨던 조건 주문)
- [ ] 복구 시나리오 테스트 (수동)
```

★이 항목은 **실거래 자금에 직결**된다. 크래시 자체보다, 크래시 후 걸려 있던 손절 주문이 사라지는 것이 위험하다. 하락장에서 손절이 실행되지 않으면 손실이 무한정 확대된다.

### 5.3.3 구조적 대응

| 원칙 | 내용 |
|---|---|
| **상태 지속화** | 실행 중 주문·조건 주문을 상태 파일에 **실시간 기록**. 메모리에만 두지 않는다 |
| **재시작 조회** | 프로그램 시작 시 (a) 상태 파일의 미체결 주문과 (b) 실제 체결내역(키움 조회)을 **대조** |
| **손절 우선 복구** | 대조 후 누락된 **손절·조건 주문을 최우선 복구** |
| **복구 테스트** | 크래시 시나리오를 재현하는 테스트를 갖춘다(수동 포함) |

### 5.3.4 이관 자산 연계

체결 대조 로직 `_run_trade_reconciliation`(키움 체결내역 vs trade_log)은 **재구현 대상**이다(`_legacy\README.md` 미이관 항목). 이 로직은 5.3.3의 (b) 재시작 조회와 직접 관련되므로, 크래시 복구 설계 시 함께 다룬다.

---

# 6. 자기진화 루프 — 모듈 경계 (R-A7)

> 대응 진단: **D6**(경계 불명확 → 영향 범위 특정 불가) · **D7**(레이어 혼재)
> 설계 명제: **각 단계가 독립적으로 검증 가능하지 않으면, 성과가 나빠도 어느 단계 탓인지 알 수 없다.** 원인을 특정할 수 없으면 개선도 불가능하다.
> ★**본 장은 인터페이스·경계만 정의한다. 각 단계의 내부 알고리즘은 비워 둔다** — 그 자리는 단권화 교재의 `[정량화] 가능` 항목이 확정된 뒤 채워진다.

## 6.0 설계 원칙

원 철학(전략 수립 → 시뮬레이션 → 결과 수집 → ML 학습 → 전략 수정)은 유지한다. 단 다음을 강제한다.

| 원칙 | 내용 |
|---|---|
| **단계 독립** | 각 단계는 앞 단계의 **출력 타입만** 알고, 내부 구현은 모른다 |
| **입출력 계약** | 단계 간 경계마다 입출력 타입·필수 필드를 계약으로 고정한다(6.6) |
| **독립 검증** | 각 단계는 다른 단계 없이 **단독으로 테스트 가능**해야 한다 |
| **상태 격리** | 단계 간 공유 상태를 두지 않는다. 데이터는 계약을 통해서만 흐른다 |

★기존 시스템에서 이 경계가 없었기 때문에, ML 학습 결과가 나빠도 그것이 (a)전략이 나쁜지 (b)시뮬레이션이 틀렸는지 (c)결과 수집이 누락됐는지 (d)학습 자체의 문제인지 분리할 수 없었다. QUART의 데이터 누수·`zip()` 절단이 겹쳤을 때 원인 규명이 어려웠던 것도 같은 구조다.

## 6.1 Stage 1 — 전략 수립

| 항목 | 내용 |
|---|---|
| 입력 | (외부) 전략 정의 — ★내부 구조는 단권화 교재 확정 후 |
| 출력 | 전략 명세 객체 — 진입·청산 조건, 파라미터, **각 파라미터의 출처**(R-S1) |
| 독립 검증 | 명세 객체가 스키마를 충족하는지, 모든 파라미터에 출처가 있는지 |
| 계약 | 출처 없는 파라미터 포함 시 → **예외**(R-A1) |

## 6.2 Stage 2 — 시뮬레이션

| 항목 | 내용 |
|---|---|
| 입력 | Stage 1 전략 명세 + 시계열 데이터 |
| 출력 | 거래 시퀀스 + 성과 지표 (**value/n/ci/power/status** 형식, R-A3) |
| 독립 검증 | 동일 입력 → 동일 출력(결정성). 슬리피지·수수료 반영 여부 |
| 계약 | 시계열 분할은 **시간 기준만**(R-A2). cutoff 이후 데이터 접근 시 예외 |
| 이관 자산 | `_v_47_v4_realistic_slippage`(슬리피지 0.76%) — 시뮬레이션 정밀도 기준. `_v_46_walk_forward_validation` — 분할 인터페이스 |

## 6.3 Stage 3 — 결과 수집

| 항목 | 내용 |
|---|---|
| 입력 | Stage 2 거래 시퀀스·성과 지표 |
| 출력 | 영속화된 결과 레코드 |
| 독립 검증 | 저장·조회의 왕복 일치(round-trip). 누락 0 |
| 계약 | `created_at` 필수, `ORDER BY` 동반(R-A5). 부분 저장 금지(R-A1) |

## 6.4 Stage 4 — ML 학습

| 항목 | 내용 |
|---|---|
| 입력 | Stage 3 결과 레코드 |
| 출력 | 학습된 모델 + 검증 지표(**status 포함**, R-A4) |
| 독립 검증 | train/test 분리의 무결성. 누수 없음 |
| 계약 | **시간 기준 분할만 사용**(R-A2). 종목 기준 분할 API는 존재하지 않는다. 검정력 미달 시 `INDETERMINATE` |

★기존 시스템의 데이터 누수와 `zip()` 절단이 모두 이 단계에 있었다. 계약이 이 두 실패를 구조로 차단한다.

## 6.5 Stage 5 — 전략 수정

| 항목 | 내용 |
|---|---|
| 입력 | Stage 4 모델·검증 지표 |
| 출력 | 수정된 전략 명세 (→ Stage 1로 환류) |
| 독립 검증 | 수정 근거의 추적성 — 어느 지표가 어느 수정을 유발했는지 |
| 계약 | `status = INDETERMINATE`인 지표를 근거로 수정하지 않는다(R-A4). 판정 불가는 **표본 축적 대상**이지 수정 대상이 아니다 |

## 6.6 단계 간 계약 (Contract)

각 경계는 다음을 만족한다.

| 검사 | 위반 시 | 근거 |
|---|---|---|
| 출력 타입이 다음 단계 입력 스키마와 일치 | 예외 중단 | R-A1 |
| 필수 필드 존재 | 예외 중단 | R-A1 |
| 길이·차원 정합 | 예외 중단 (묵시적 절단 금지) | R-A1 |
| 결과에 `status` 동반 | 예외 중단 | R-A4 |
| 시간 경계 침범 없음 | 예외 중단 | R-A2 |

★**계약 위반은 조용히 통과하지 않는다.** 이것이 5개 단계 전체에 R-A1을 관철하는 지점이다. 계약이 없으면 한 단계의 결함이 다음 단계로 전파되어, 최종 결과에서야 드러난다. 그 시점에는 원인이 이미 여러 단계 뒤에 있다 — D6의 "영향 범위 특정 불가"가 정확히 이것이다.

---

# 7. 거버넌스 (ADR)

## 7.1 ADR 도입

설계 변경·핵심 결정마다 **ADR(Architecture Decision Record)**을 남긴다.

| 필드 | 내용 |
|---|---|
| 결정 | 무엇을 정했는가 |
| 근거 | 왜 — **D#/R-A#/검증 결과/사용자 지시** 중 하나에 연결 |
| 대안 | 고려한 다른 선택지와 기각 사유 |
| 영향도 | 이 결정이 바꾸는 범위 |

## 7.2 원칙 연결

ADR은 프로젝트 기본 원칙 *"모든 값에는 출처가 있어야 한다"*(R-S1)의 **구조적 구현**이다. 코드의 파라미터에 출처를 요구하듯, 설계의 결정에 근거를 요구한다.

★근거 필드가 D#/R-A#/검증/사용자 지시 중 어디에도 연결되지 않는 결정은 **보류**한다. 이는 본 설계서 0.3(추적 원칙)을 결정 수준으로 확장한 것이다.

## 7.3 부채 목록

5.2·R-A10에서 등록되는 모든 우회를 관리하는 대장.

| 필드 | 내용 |
|---|---|
| 유형 | 로직 우회(R-A9) / 검증 우회(R-A10) / 크기 상한 예외(R-A8) |
| 위치·사유 | 어디서 왜 |
| 등록일·기한 | 해소 기한 명시 |
| 상태 | 미해소 / 해소 / 기한 초과 |

★기한 초과 항목은 검증 산출물에 경고로 노출된다(2.2.4).

---

# 8. 부록

## 8.1 R-A ↔ 절 대응표

| 항목 | 요구 | 배치 절 |
|---|---|---|
| R-A1 | 조용한 통과 금지 | 2.1 / 6.6 |
| R-A2 | 시계열 분할 강제 | 4.1 / 6.2 / 6.4 |
| R-A3 | 표본·검정력 동반 | 3.1 / 6.2 |
| R-A4 | 3값 결과 | 3.2 / 6.4 / 6.5 |
| R-A5 | 상태 추적 | 4.2 / 6.3 |
| R-A6 | API 계층화 | 4.3 |
| R-A7 | 루프 단계 독립 검증 | 6장 전체 |
| R-A8 | 모듈 크기 상한 | 5.1 |
| R-A9 | 임시 우회 금지 | 5.2 / 7.3 |
| R-A10 | 검증 우회 차단 | 2.2 / 7.3 |
| R-A11 | 크래시 복구 | 5.3 |

**11/11 배치 · 누락 0**

## 8.2 이관 자산 활용 현황

| 자산 | 연결 절 | 지위 |
|---|---|---|
| `check_integrity.py` | 2.3 | 강제 지점 연결 |
| `pre_commit_check.py` | 2.3 / 5.1 | 커밋 게이트 (연동 미완 확인 필요) |
| `snapshot_test.py` | 2.3 | 대조 게이트 |
| `_v_46_walk_forward_validation` | 4.1 / 6.2 | 분할 인터페이스로 승격 |
| `_v_47_realistic_slippage` | 6.2 | 시뮬레이션 정밀도 기준 |
| `_run_trade_reconciliation` | 5.3.4 | 재구현 (미이관) |
| `PHASE_1A_DATA_SOURCES.md` 서술 | 3.3 | 문체 선례 계승 |

## 8.3 미결 사항

- **A1.** 모듈 크기 상한 수치 미정 — 기존 정상 모듈 분포 조사 또는 사용자 지시 필요(5.1)
- **A2.** 표본 부족 임계 미정 — 검정력 계산 또는 사용자 지시(3.1.4)
- **A3.** `pre_commit_check.py` 연동 미완 항목(ROADMAP §0-5)의 현재 상태 미확인
- **A4.** 각 Stage 내부 알고리즘 — 단권화 교재 `[정량화] 가능` 항목 확정 후
- **A5.** 기술 스택 선정 — 구조 확정 후 별도 논의
- **A6.** `_v_01~_v_67` 개별 재사용 가치 미평가(M3)

---

*(설계서 초안 완료. 본문은 정독 완료·단권화 교재 확정 이후 각 Stage 내부를 채우며 확장된다.)*
